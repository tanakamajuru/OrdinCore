/**
 * Deliberately virtual catch-all. It is returned for every active governance
 * theme, but is not stored as a normal taxonomy row and cannot be promoted as
 * a reusable label without human review.
 */
export const OTHER_SIGNAL_LABEL = 'Other concern within this theme';
